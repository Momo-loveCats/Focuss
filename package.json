{
    "name": "Focuss",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
      "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:serveur\"",
      "dev:frontend": "cd frontend && npm run dev",
      "dev:backend": "cd serveur && npm run dev",
      "build": "npm run build:frontend && npm run build:serveur",
      "build:frontend": "cd frontend && npm run build",
      "build:backend": "cd serveur && npm run build",
      "install:all": "npm install && cd frontend && npm install && cd ../serveur && npm install",
      "type-check": "cd frontend && npm run type-check && cd ../backend && npm run type-check"
    },
    "devDependencies": {
      "concurrently": "^8.2.0"
    }
  }